using System;

class Program
{
    static void Main()
    {
        // Заданный массив строк с цитатами
        string[] quotes = {
            "Я не могу просто так взять и... А, ладно, возьму и сделаю.",
            "Когда ты понял, что жизнь — это не только про пиццу и котиков.",
            "Почему мой код не работает? Потому что я программист, а не волшебник.",
            "Не будь как буратино, думай головой!",
            "Кофе — лучший друг программиста.",
            "Работаешь удаленно? Значит, ты дома в пижаме.",
            "Если бы у меня был рубль каждый раз, когда кто-то говорит 'Это просто', я бы уже купил себе яхту.",
            "Кто рано встаёт, тот устаёт раньше других.",
            "Моя жизнь — это бесконечный поток багов и фич.",
            "Понедельник — день тяжёлый, вторник — день весёлый.",
        };

        // Использование цикла foreach для прохождения по массиву
        foreach (string quote in quotes)
        {
            // Проверка длины строки и наличия точки в конце
            if (quote.Length <= 50 && quote.EndsWith("."))
            {
                Console.WriteLine(quote);
            }
        }
    }
}
